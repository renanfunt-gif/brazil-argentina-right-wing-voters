# Raw data instructions

Upload the LAPOP microdata files here using these exact filenames:

- `lapop_brazil_2018_2019.dta`
- `lapop_argentina_2023.dta`

After upload, open `config/variable_mapping.csv` and replace placeholder variable names with the exact variable names used in your files.
